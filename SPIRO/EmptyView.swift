import SwiftUI
import Charts
import CoreXLSX

struct EmptyView: View {
    var body: some View {
        Text("This is an empty screen.")
            .font(.title2)
            .foregroundColor(.gray)
    }
}
